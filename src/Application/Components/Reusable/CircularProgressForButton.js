import { CircularProgress } from "@material-ui/core";
import React from "react";

const CircularProgressForButton = () => {
  return (
    <CircularProgress
      style={{
        width: "36px",
        height: "36px",
        marginRight: "22px",
      }}
    />
  );
};

export default CircularProgressForButton;
